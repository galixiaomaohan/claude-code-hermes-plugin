// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../../../resource.mjs";
import { isRequestOptions } from "../../../../core.mjs";
import { Page } from "../../../../pagination.mjs";
export class Messages extends APIResource {
    /**
     * Adds a message to a session
     */
    create(appId, userId, sessionId, body, options) {
        return this._client.post(`/v1/apps/${appId}/users/${userId}/sessions/${sessionId}/messages`, {
            body,
            ...options,
        });
    }
    /**
     * Update the metadata of a Message
     */
    update(appId, userId, sessionId, messageId, body, options) {
        return this._client.put(`/v1/apps/${appId}/users/${userId}/sessions/${sessionId}/messages/${messageId}`, {
            body,
            ...options,
        });
    }
    list(appId, userId, sessionId, params = {}, options) {
        if (isRequestOptions(params)) {
            return this.list(appId, userId, sessionId, {}, params);
        }
        const { page, reverse, size, ...body } = params;
        return this._client.getAPIList(`/v1/apps/${appId}/users/${userId}/sessions/${sessionId}/messages/list`, MessagesPage, { query: { page, reverse, size }, body, method: 'post', ...options });
    }
    /**
     * Bulk create messages for a session while maintaining order. Maximum 100 messages
     * per batch.
     */
    batch(appId, userId, sessionId, body, options) {
        return this._client.post(`/v1/apps/${appId}/users/${userId}/sessions/${sessionId}/messages/batch`, {
            body,
            ...options,
        });
    }
    /**
     * Get a Message by ID
     */
    get(appId, userId, sessionId, messageId, options) {
        return this._client.get(`/v1/apps/${appId}/users/${userId}/sessions/${sessionId}/messages/${messageId}`, options);
    }
}
export class MessagesPage extends Page {
}
Messages.MessagesPage = MessagesPage;
//# sourceMappingURL=messages.mjs.map